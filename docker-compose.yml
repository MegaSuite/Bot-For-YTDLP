services:
  bot-for-ytdlp:
    build:
      context: .
    restart: always
    environment:
      - TELEGRAM_BOT_KEY=
      - YOUTUBE_API_KEY=
    volumes:
      - ./configs:/app/telegram_youtube_downloader/configs
      - ./downloads:/app/downloads
      - ./logs:/app/logs